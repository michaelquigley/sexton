project_name: sexton

builds:
  - main: ./cmd/sexton
    binary: sexton

changelog:
  sort: asc
  filters:
    exclude:
      - '^docs:'
      - '^test:'
      - '^ci:'
      - Merge pull request
      - Merge branch

release:
  github:
    owner: michaelquigley
    name: sexton
  draft: false
  prerelease: auto
