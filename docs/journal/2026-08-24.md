# 2026-08-24

## commit-policy arc, pre-handoff self-audit

- michael selected fresh codex for the build (house default; the nine-round convergence is the argument — plan-precision this high wants a plan-follower, with terminus attention concentrated on stage 3 and the git-plumbing half of stage 2).
- top-to-bottom coherence re-read of both artifacts after the nine editing rounds. six drift finds, all editorial residue from overlapping edits, none touching decided behavior:
  - spec: a stale "crash leaves the mechanical message" sentence contradicting the round-8 "crash leaves the placeholder" claim two sentences earlier (round-4 edit left the round-3 sentence standing); the mermaid commit node lacked the reword step; "setAttention is a sibling of setError" softened to "borrows two behaviors" — round 3 made the state handling a deliberate divergence.
  - work order: a grounding note still said the spec's rename sentence was "flagged for a one-line spec correction" (applied same-day, and superseded twice since); "not just the obvious five" dangled after its MADRC referent was deleted; the critical-files table lacked the `internal/rpc` + `cmd/sexton/status.go` row for the detail-column work; one stray `— →` punctuation glitch.
- fixes applied and unfurled. content is otherwise what `ready_to_build` reviewed; these repairs change no mechanism, test, or decision.
- michael committed the converged artifacts (`908a3a5`) before the audit fixes landed, so the audit repairs ride uncommitted with stage 1.

## commit-policy stage 1 review

- codex's stage 1 (config model and resolution) landed; reviewed against the work order's stage-1 contract in this session. complete: fields/constants, default-to-`none` + `PolicyDefaulted`, unknown-value rejection, region list-replace, full normalization (near-miss pinned), regions-without-regions error, malformed-`.sexton.yaml` handling. `make test` + vet green.
- implementation choices reviewed and endorsed:
  - forced `none` rides the substituted *local layer* (`RepoLocalConfig{CommitPolicy: PolicyNone}`) — highest precedence, nothing overrides it; `PolicyDefaulted` stays false so the stage-4 warning will say malformed, not unconfigured (container test pins the distinction).
  - malformed region entries error even when the list is inert under `all`/`none` — stricter than the spec's letter, right strictness: inert = valid-but-unused, not garbage-tolerated.
- terminus `10415fda35c1` (rubric, working tree): **clean**, zero findings — a sexton rubric now resolves from the canon, so the gate is no longer ad-hoc. stage 1 awaits michael's acceptance; per the 08-17 lesson, a clean round is sampled coverage, not proof — stages 2–4 each get their own.

## commit-policy implementation, stage 1

- configuration model landed for all three cascade layers: `commit_policy`, `commit_regions`, the `all`/`regions`/`none` constants, and resolved `PolicyDefaulted` / `LocalConfigError` state. no `yaml:` tags; real-YAML tests cover repo-local, global-entry, and global-default binding through `df/dd`.
- policy resolves repo-local → global repo entry → global defaults; missing policy becomes fail-safe `none` with `PolicyDefaulted` set. region lists replace by first non-empty layer and normalize to clean repo-relative prefixes with a trailing slash; empty, absolute, whole-repo, escaping, unknown-policy, and regions-without-paths configurations fail resolution. inert regions under `all`/`none` remain accepted.
- a malformed `.sexton.yaml` is now represented as an explicit repo-local `none` before resolution, so a lower-layer `all` cannot bleed through. the parse error stays on `ResolvedRepo.LocalConfigError` for stage 4's distinct startup warning. the container integration test pins malformed local + global `all` → visible repo + forced `none`.
- `make test` green. terminus ran twice because path-mode cannot seed an untracked test file: tracked stage paths clean in `f6ccd11823bd`, then the complete working-tree stage including `container_test.go` clean in `2cdde1cfebad`; both zero findings. no third round needed.
- built-behavior docs and changelog intentionally untouched: the work order declares intermediate stages non-deployable, and the user-facing policy/default flip lands with stage 4.

## commit-policy implementation, stage 2

- the git status read is now `--porcelain=v1 -z -b -uall`. `Status.Entries` retains raw `Path` / `OldPath` endpoints and index/worktree codes for policy partitioning; legacy category lists remain the message/alert surface, escaping control-bearing or invalid-UTF-8 names before they leave the git layer. real-git tests pin staged renames in both directions across a region boundary; parser tests cover spaces, quotes, backslashes, newlines, literal ` ->`, and variable-arity `R100` / `C75` name-status records.
- pull preflight now asks `IsDirtyTracked` (`status --porcelain -uno`): untracked-only dirt no longer blocks a rebase, while worktree-modified and index-staged tracked dirt still return `ErrDirtyWorkingTree` before any network call.
- `StageRegions` and `CommitOnly` share `:(literal)` region pathspecs. integration tests cover adds/modifications/deletions, pathspec metacharacters, operator-staged outside work surviving unswept, and a staged cross-boundary rename committing the in-region tree endpoint while its out-of-region endpoint remains staged.
- commit creation now returns the exact object id parsed from Git's own commit summary, expands it without consulting mutable `HEAD`, and verifies the object message. `Show`, `ShowStat`, and NUL-parsed `ShowNameStatus` read that object with `--format=` and `--diff-merges=first-parent`; a real merge whose result matches its second parent proves all three still describe what the merge brought onto the first parent.
- `RewordCommit` reads the old object's tree, every parent, and author identity/time; `commit-tree` builds the replacement and `update-ref <new> <old>` swaps it atomically. tests pin tree/parent/author preservation, staged content remaining untouched, and refusal after an operator commit moves the branch — the operator commit and sexton's placeholder both remain unchanged.
- `gitClient` and its test double carry the stage-2 primitives. `DiffStaged` / `DiffStat` remain temporarily callable because the stage-2 sync loop still generates before committing; stage 3 removes that legacy path while implementing the settled commit-first/object-derived sequence. no intermediate stage is deployable.
- focused real-git tests and `make test` + vet green. terminus rubric review `0ac5a3ef6828`: clean, zero findings on round 1; no further rounds needed. built-behavior docs and changelog remain stage 4 work.

## commit-policy stage 2 review

- codex's stage 2 (git layer) reviewed against the work-order contract in this session: complete and faithful. command shapes verbatim; `GIT_SSH_COMMAND`-appended-last and nil-`cmd.Env` behavior survived the `runCtxEnv` refactor; SHA capture is parse-then-verify with color disabled, taking the last summary match; `RewordCommit` preserves tree/parents/author via env-injected raw dates and refuses via CAS. above-the-letter touches: the reword test also pins index preservation, and `parseNameStatus` errors on truncated records instead of misaligning.
- my terminus round (`2026479ce8ed`): clean verdict with two advisories, both taken with michael's approval — `newStatus` → `NewStatus` (constructor visibility matches the exported type), and `TrackedPaths` now includes a rename's old path when *either* column carries `R`, with a worktree-side rename test (copies stay excluded; their old path is an unchanged source). notable: the `Y == 'R'` gap was an edge i had read past as acceptable — the rubric caught what the human-side review waved through.
- re-review after the fixes (`c9855d97739c`): clean, zero findings. `make test` + vet green throughout.
- carry-forward for the stage 3 checklist: pin that the partition ignores a porcelain `C` entry's old path — the copy-exclusion rule is pinned on the name-status side and in `TrackedPaths`, but the partition's own treatment gets its real test where the partition lives.

## commit-policy implementation, stage 3

- the sync loop now reads and partitions raw status entries on every cycle. `all` owns every changed endpoint, `none` owns none, and `regions` selects only the configured prefixes that own dirt. rename endpoints classify independently while a copy's unchanged source stays outside the partition; tracked out-of-region dirt pauses pulls, untracked-only dirt does not, and pushes continue.
- commits now land before they are described: lifecycle pre-hook, policy-scoped staging, a count-free placeholder commit, exact-SHA `ShowNameStatus` / `Show` / oversized `ShowStat`, compare-and-swap reword, then the post-hook. fallback messages and completion file lists come from the created object rather than the mutable working tree, and completion alerts no longer attach a potentially unrelated terminal SHA. terminal `ShortHEAD` remains the status value.
- `attention` is a distinct resting state. detail strings sort and deduplicate raw paths, quote every path unconditionally, cap the visible set at ten, and identify when pulls are paused. identical standing conditions alert once; changed conditions re-alert; the first clean partition clears and announces resolution.
- all terminal transitions now converge through one locked chooser: holdout, active request-time snooze, error, attention, watching. this preserves hidden error/attention details beneath pauses, prevents a mid-cycle attention alert from masking `Syncing`, honors snoozes arriving in blocked phases, and lets a snooze that expires in flight fall through without restarting its clock. successful recovery detection now captures the standing error at cycle entry, fixing the previously suppressed `recovered from error` alert.
- mock tests cover all policies, literal and cross-boundary region behavior, copy exclusion, attention collisions/capping/dedup/recovery, compound error precedence, expired-snooze and holdout transitions, remote-boundary snoozes, and the mid-cycle dirty race. real-git tests prove a lifecycle hook cannot sweep newly staged out-of-region work into a partial commit, a pre-hook addition shapes the exact-object fallback, and an edit made during the LLM request does not enter the message or completion metadata.
- `make test` (including vet) and `go test -race ./internal/agent` are green. built-behavior docs and changelog remain intentionally deferred to stage 4, when the attention surfaces and startup warnings make the behavior deployable.
- terminus round 1 (`7453cb0d0479`) was clean with zero findings. round 2 (`465206976406`) was clean with one truthful-naming advisory: the partition fields spoke in region terms even though the type also represents `all` and `none`. michael accepted the mechanical correction; the fields and their uses now say selected / unselected, and the full test, vet, and race gates remain green after the rename.
- terminus round 3 (`20cdb4497a20`) found one stale warning that still called the exact-object `Show` result a staged diff. michael accepted the correction to `commit diff`; the diagnostic now names the immutable source it actually reads.
- post-fix terminus review `d7d9408dc792`: clean, zero findings. stage 3 is ready for michael's review and commit.

## commit-policy stage 3 review

- codex's stage 3 (sync-loop rework) reviewed against the work-order contract in this session: faithful on every load-bearing surface. `restingStateLocked` is the sole rest-state assigner across all eleven call sites; `hadError` captured at cycle entry fixes the shipped recovery-suppression defect for real; the partition is per-endpoint with copy sources excluded and hands `StageRegions` only regions owning dirty paths; the attention detail is unconditionally quoted with metadata outside the tokens; the completion alert carries no SHA while `lastCommit` keeps terminal HEAD honestly; commit-first with placeholder → describe-by-SHA → reword-on-every-path is exactly the settled sequence.
- the vocabulary rename (selected/unselected) was terminus round 2's advisory, taken with michael's approval after my anchored-veto proposal was rightly rejected — comments don't anchor vetoes (the 08-17 measurement), and "selected" generalizes better if glob regions ever land. the work order's in-region language stays as historical record; stage 4's docs/current standardizes on the code's vocabulary.
- **accepted residual, michael's call:** a refused reword CAS (operator commit during the LLM window) surfaces as `setError("commit reword failed")` and recovers next poll — error-plus-recovery noise for a definitionally benign race. accepted as-built; the placeholder message stands either way, and the simplicity beats a sentinel classification. recorded in the spec's error-by-tier census beside the non-fast-forward compound, revisit-if-noisy.
- verdicts: my full-diff read (one finding, the residual above), codex's three terminus rounds (two advisories taken along the way), my sampled fourth round `00c860c335fd` clean zero findings. `make test`, vet, race all green. stage 3 awaits michael's commit; stage 4 (surfaces) is last.

## commit-policy implementation, stage 4

- attention is now a complete alert severity: the log sink writes it at warning level, Mattermost prepends only configured `mention_users`, and non-attention severities ignore that list. repo names, messages, errors, commit messages, status cells, and filenames are rendered with markdown and mention tokens neutralized; control bytes and invalid UTF-8 remain visible escapes, while backtick-bearing filenames use a safe variable-length code-span delimiter.
- alert delivery failures are no longer discarded. both agent alert paths emit a local warning containing the repo, undelivered message, and sink error. startup emits exactly one warning per repo when policy defaulted to `none`; a malformed repo-local config uses the stronger forced-`none` warning instead.
- retained attention now crosses both control-plane adapters and the protobuf `RepoStatus.attention_detail` field. CLI and Mattermost status tables expose one `detail` column, preferring retained error over attention and continuing to show that detail while snooze or holdout masks the underlying state.
- Mattermost `mention_users` binds through a real YAML load and remains egress policy: alert entries sharing a client identity may configure different recipients, while the existing `allowed_users` / `trigger_words` ingress-consistency rule stays unchanged.
- README, changelog, and all affected `docs/current/` authorities now describe commit policies, the fail-safe default and fleet migration, selected/unselected partitioning, attention and pull behavior, exact-object commit messages, Mattermost mentions, and status detail semantics. protobuf stubs were regenerated with `protoc-gen-go` 1.36.11 and `protoc-gen-go-grpc` 1.6.1.
- focused surface tests, `make test` with vet, and `go test -race ./...` are green. hostile-path coverage follows escaped newline, pipe, backtick, mention-like text, and invalid bytes through protobuf and both status renderers. terminus round 1 (`4351f750ff0e`) was clean with zero findings; no further round needed.

## commit-policy stage 4 review

- codex's stage 4 (surfaces + docs) reviewed against the work-order contract in this session: complete. two-variant startup warning with malformed outranking unconfigured; delivery-failure logging in both alert paths; attention at warn level in the log sink; `mention_users` real-YAML tested and treated as egress beside `channel_id`; the mention-neutral rendering applied wider than the contract asked (repo names, status cells, commit messages, error text; variable-length code-span delimiters against embedded backticks); `detail` column renamed in both tables with error-else-attention selection covering the paused-row advisory; proto field via genuine protoc regen; changelog FEATURE + breaking CHANGE in house format.
- the `docs/current/` synthesis is accurate to built behavior at every cross-checkable point — sync-loop's partition phase order, the chooser's complete precedence, attention's mid-cycle state discipline, the recovery fix, never-restarted snooze timers, the no-SHA completion rationale, copy-source exclusion, egress/ingress dedup — all in the selected/unselected vocabulary, standardizing as agreed.
- verdicts: codex's terminus round `4351f750ff0e` clean; my sampled round `ecd9ea51f192` clean, zero findings. `make test`, vet, race green.
- all four stages now reviewed and terminus-clean. remaining end-of-arc work after michael's commit: retire `docs/future/commit-policy.md` + `-work-order.md` (re-synthesize the deferred section into small future docs; the census residuals already flowed into docs/current via the spec), canon drift check against the landed code, and the operator's roadmap triage of the two originating cards.

## commit-policy end-of-arc ceremony

- residuals synthesized into `docs/current/`: the two accepted error-compounds (non-fast-forward while pulls paused; refused reword swap) added to sync-loop.md beside the not-errors list, revisit-if-noisy; the native-hooks deferral added to git-and-commit-messages.md with its revisit closures.
- deferred items carded: `roadmap/chat-ops-commit-acceptance.md` (feature), `roadmap/glob-commit-regions.md` (enhancement), `roadmap/mention-users-on-error-alerts.md` (enhancement). push gating needed no card — it is a decision record, carried by configuration.md's "policies affect commit creation, not pushing".
- intent documents retired: `docs/future/commit-policy.md` and `-work-order.md` deleted (history in git; committed at `908a3a5` and refined through the stage commits). `mercurius.yaml` and `.mercurius/` removed per the arc-close convention — nine rounds of logs, value spent.
- **canon drift check** (read against landed code; flagged, not edited — canon changes happen in terminus-canon deliberately):
  - `never-discard-working-tree`: no clause covers branch-ref rewrites. `RewordCommit` moves the branch via `update-ref <new> <old>`, and the compare-and-swap against sexton's own just-created commit is the entire safety of it — a diff dropping the expected-old argument (unconditional `update-ref`) could rewrite an operator's commit and sails past the quality's current flag list (reset/checkout/clean/stash/force-push/`branch -D`/`rebase --skip`). a real invariant with no clause.
  - `non-invasive-to-synced-repos`: the discrimination clause "flag an environment construction that hands the child a non-empty environment when no ssh key is configured" is now wrong-shaped. the reword path legitimately injects `GIT_AUTHOR_*` atop `os.Environ()` with no ssh key — ssh-agent inheritance is preserved because the inherited environment is included. the clause should key on *losing `os.Environ()` inheritance* or last-wins ordering, not on non-emptiness.
  - `no-yaml-tags-on-dd-bound-types` and the rubric characteristics: no drift.
- the arc is closed. remaining operator moves: commit the ceremony diff, triage the two originating roadmap cards (`auto-commit-regions`, `pull-only-repos`, both realized) and the three new ones, and take the two drift flags to terminus-canon.
