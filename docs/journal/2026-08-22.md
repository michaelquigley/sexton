# 2026-08-22

## commit-policy arc, outage and continuation

- a ~30-hour power outage interrupted the session mid-arc; the mercurius server died with it, orphaning the original round 8 before the reviewer ran (round-08 dir has snapshots only, no result). validated before continuing: journal accurate through round 7, rounds 1–7 complete on disk with 33 recorded decisions, current artifacts byte-identical to the orphaned round's launch snapshots, mercurius.yaml calibration and all six guards intact.
- mercurius sessions are in-memory: the old id `s_Mp2lwCCls58E` is dead; the arc continues under `s_OeiduogawoUK`, whose round numbers restart at 1. arc round = session round + 7. the old session dir remains the audit trail.

## commit-policy arc, mercurius round 8 (new session round 1)

- verdict `needs_changes`: one concern, two advisories, all taken; reviewer calls everything else implementation-ready.
- CP-R8-001: the `sync complete (<ShortHEAD>)` alert read HEAD at cycle end — after the pull's rebase (which rewrites sexton's commit) or an operator commit on top, the SHA names someone else's commit while the attached message/files describe sexton's captured object. the file-bearing alert now carries no SHA, its metadata labeled as sexton's created change; the status table's last-commit column keeps terminal HEAD under its honest repo-HEAD-after-sync meaning. neither the captured nor the terminal SHA can be both attached and always-true — dropping the decoration was the smallest honest fix.
- advisories: repo name rendered mention-neutral in alerts (a repo named `@channel` would ping); paused rows (`snoozed`/`holdout`) show the highest-precedence retained detail so a masked error/attention never reads as clean.
- next round launched.

## commit-policy arc, mercurius round 9 (new session round 2): ready_to_build

- **verdict `ready_to_build`, zero concerns, zero advisories.** the reviewer affirms the partial-commit boundary, hostile-path parsing, rename endpoint semantics, pull pausing, commit-object-derived messaging, atomic rewording, attention/error precedence, configuration cascade, and operator-facing surfaces as settled with proportionate integration tests.
- the arc: nine rounds across two sessions (seven in `s_Mp2lwCCls58E`, two in `s_OeiduogawoUK`), 36 recorded dispositions — 34 fixed, 1 deferred with a settled-decisions guard (native hooks), 1 the operator reframed mid-triage (in-repo-only migration). two structural inversions survived review: the partial-commit boundary (round 1) and commit-first-describe-after with CAS reword (rounds 3–4). two shipped defects found incidentally, folded into the work order: the suppressed `recovered from error` announcement and discarded alert-delivery errors.
- artifacts converged: `docs/future/commit-policy.md` + `docs/future/commit-policy-work-order.md`, uncommitted — the review queue. next: michael's review/commit, then implementation-agent selection; `mercurius.yaml` and `.mercurius/` come out when the artifacts land, per convention.
