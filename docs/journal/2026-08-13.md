# 2026-08-13

## roadmap adoption

- **The stock `## Roadmap` paragraph in `AGENTS.md` is verbatim text with the line wrapping removed.** The grimoire's copy is hard-wrapped at ~75 columns; `unfurl -i` collapsed it on the way in, which is correct — unfurl is unconditional on authored markdown and the rest of this file is unwrapped. The words are byte-identical to the convention. Don't "restore" the wrapping to match sibling repos; it isn't drift.

- **`docs/future/semantic-differs.md` is kept deliberately, not folded into its roadmap card.** The card is the prompt and points at the doc; the doc holds the March-era design. Its *implementation plan* half describes a codebase that has since moved (hooks, holdout, the control-plane split all landed after it was written) — re-ground it against current internals before it becomes a work order, rather than trusting its function signatures. Consolidating card and doc is an open call for Michael, not settled.

- **Board registration lives outside this repo.** `~/.config/ranger/config.yaml` gained a `sexton` root, which leaves no trace in the tree — if the board can't see sexton on another host, that file is why. Nothing in the convention depends on it; the files are the truth.

- **No `order.yaml` was written.** Both seed cards land in `inbox` unranked; priority and lane are the operator's gestures at triage.
