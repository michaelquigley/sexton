# 2026-08-21

## commit-policy arc, mercurius round 1

- session `s_Mp2lwCCls58E`, codex/gpt-5.6-sol, verdict `needs_changes`: six major concerns + one advisory, all accepted and applied to both artifacts. round 2 launched after calibration upkeep (review_focus still named the index guard; replaced with the partial-commit boundary; added `diff-cap-deferred` guard).
- the big structural change (CP-R1-001): the region boundary moved from a racy pre-commit index check to the commit itself — `CommitOnly` does `git commit -- :(literal)<region>` (partial commit, git's default `--only` mode), which builds the commit from the named paths regardless of index state. the index guard died; operator-staged out-of-region work is just tracked dirt that raises attention. michael had not known partial commits existed.
- CP-R1-002: status read switches to `--porcelain=v1 -z` — no quoting exists in that format, so the planned C-unquoting work was deleted, not fixed. membership rule pinned fail-safe: in-region only when every endpoint (rename `OldPath` included) is in-region.
- CP-R1-003: the oversize LLM fallback (`DiffStat`) was whole-tree; becomes `--staged --stat`. michael questioned the 32KB cap itself (200k-context model); cap stays, configurability carded → `roadmap/configurable-llm-diff-budget.md`.
- CP-R1-004 reframed mid-triage: michael killed the global-`all` migration — policies are in-repo `.sexton.yaml` only, no host config changes (4 repos, 4 hosts). the hardening still applied: a malformed `.sexton.yaml` forces `none` and never resolves policy from lower layers, distinct startup warning.
- two existing shipped defects surfaced by review, both folded into the stages that rebuild the relevant logic:
  - `recovered from error` never fires: `sync()` checks `state == Error` at cycle end but its own top set `Syncing`. the existing test pins the suppression; sync-loop.md documents the announcement as working. fix in stage 3 (capture error detail at cycle entry; `restingStateLocked` chooser at all five return-to-rest sites incl. holdout exit and cancel cleanup).
  - alert delivery failures are invisible: `Agent.alert` discards the alerter's error, contrary to mattermost.md's "failed PostMessage is logged" claim. fix in stage 4 (central delivery-failure logging).
- correction from michael during CP-R1-006 discussion: sexton does not automatically restart on hosts — don't build scenarios on that premise.

## commit-policy arc, mercurius round 2

- verdict `needs_changes`: three major concerns — all corner-case fallout from round 1's fixes — plus two advisories; everything accepted and applied.
- CP-R2-001: round 1 moved the commit boundary but left the diff reads behind — with operator-staged out-of-region entries, `DiffStaged`/`DiffStat` read the whole index while `CommitOnly` commits region paths only. now one `:(literal)` pathspec list drives staging, both diffs, and the commit.
- CP-R2-002: attention clearing lived in the dirty branch, but the operator's fix (committing) produces a *clean* cycle — attention would stand forever. partition now evaluates every cycle; clean tree = empty partition, clears and announces.
- CP-R2-003: round 1's every-endpoint rename rule fought git's tree semantics — `git commit -- journal/` takes the in-region half of a staged cross-boundary rename regardless of labels, so committed content depended on unrelated dirt. membership is now per-endpoint (in-region endpoint → commit set and file lists; out-of-region endpoint → attention and pull-pause); staged and unstaged moves behave identically.
- advisories: migration section documents rollout ordering (policy files land, then daemons restart; interim is fail-safe none-with-warning); the journal-evidence overclaim softened. per michael, live adoption of pulled `.sexton.yaml` changes carded → `roadmap/adopt-repo-local-config-changes.md`.

## commit-policy arc, mercurius round 3

- verdict `needs_changes`: four majors in deep git semantics, two advisories. michael is running rounds until `ready_to_build`.
- CP-R3-003 produced the arc's second structural inversion: **commit first, describe after**. the pathspec commit takes worktree content at commit time, so the LLM-call window let the commit drift from its message. now: partial commit lands immediately with the mechanical message, LLM message generated *from the commit* (`git show`), `AmendMessage` (`git commit --amend --only -m`, message-only reword of the own unpushed commit) before any push. supersedes round 2's DiffStaged/DiffStat pathspec-scoping — reading the commit is bounded by construction. tripwire test pins that the reword can't absorb freshly staged content.
- CP-R3-001 **deferred by michael**: native git hooks can stage into a partial commit's temp index, but the fleet's repos carry no native hooks — no machinery for a case that doesn't exist. residual + revisit conditions (hooksPath disable, temp-index plumbing) in the spec's deferred section; `native-hooks-deferred` guard in mercurius.yaml. the 08-17 lesson applied: the veto has a durable home.
- CP-R3-002: setAttention must not flip state mid-cycle — snooze detects an in-flight sync via `state == Syncing`; a mid-cycle `Attention` state let a snooze display `snoozed` while the cycle pushed. state transitions now live solely in `restingStateLocked`.
- CP-R3-004: hostile filenames vs display surfaces — proto strings require valid UTF-8, tables split on newlines/pipes. raw bytes confined to partitioning; `strconv.Quote` form in details; render-time markdown escaping; tests through RPC marshal and both renderers.
- advisories taken: old-path partitioning is `R`-only (copies keep their unchanged source out); `restingStateLocked` also at `Resume`/`TriggerSync` expired-snooze branches.

## commit-policy arc, mercurius round 4

- verdict `needs_changes`: four majors — three stress-testing round 3's commit-first flow, one snooze/error interaction — plus two advisories. all taken.
- CP-R4-001: the amend targeted mutable HEAD — an operator commit during the LLM window would get *reworded by sexton*. now: by-SHA `Show*` reads pinned to the created object (id captured from the commit invocation's output, verified), and `RewordCommit` = `commit-tree` + CAS `update-ref` that refuses if the branch moved. refused swap leaves the placeholder. new invariant: sexton can never reword a commit it did not create.
- CP-R4-002: the mechanical fallback message carried pre-staging counts a `pre_commit` hook or commit-time drift could falsify. now: count-free placeholder at commit; every final message (LLM or mechanical) derives from the committed object and lands via the CAS reword.
- CP-R4-003: a pending snooze died at terminal error exits — ping → operator snoozes mid-push → push fails → setError → polls continue despite the pause. setError now honors holdout and pending/manual snooze before choosing state; error detail retained beneath.
- CP-R4-004: `strconv.Quote` leaves `@channel` as a live mention — the escape work covered the status table but not the alert itself. FormatAlert detail rendering is now mention/markdown-neutral; configured mentions are the only live ones.
- advisories: chooser rule made absolute (no code assigns a rest state except `restingStateLocked`; `Resume`'s post-error-clear path was the third hand-found site — the absolute rule ends that class); stages declared implementation checkpoints — nothing deploys before stage 4 completes.

## commit-policy arc, mercurius round 5

- verdict `needs_changes` but visibly narrowing: two majors, two advisories, all taken; the reviewer's summary now calls the boundary, messaging, cascade, and ordering well developed.
- CP-R5-001: the chooser closed error exits (round 4) but not *successful* ones — a snooze/holdout landing after the last abort checkpoint got overwritten by `completeSync`. final form: `restingStateLocked` embodies the complete precedence (holdout → pending/active snooze → error → attention → watching) under the lock, at every terminal path. five rounds of exit-by-exit patching ended by making the chooser own the pause states itself.
- CP-R5-002: the attention detail is the dedup key, and conditional quoting let distinct conditions render identically (`a, b` as one file vs two; a file named `foo (pulls paused)` impersonating the suffix) — silently swallowing re-alerts. now: unconditional `strconv.Quote` on every token, metadata outside the quotes, whole-string injectivity.
- advisories: `ShowNameStatus` goes `-z` (default `--name-status` reintroduces C-quoting through the back door); `RewordCommit` tests pin tree/parents/author preservation — `commit-tree` inherits none of it automatically.

## commit-policy arc, mercurius round 6

- verdict `needs_changes`, narrowest yet: two majors the reviewer itself called small documentation fixes, one advisory. all taken.
- CP-R6-001: my round-5 wording said the chooser "starts the timer" on a pending snooze — contradicting the API, which starts it at request time and returns that expiry. fixed: chooser applies a pending snooze only while `snoozeUntil` is still future, never touches timers; outlasted-snooze test added.
- CP-R6-002: the status tables' detail column is literally labeled `error` — attention detail would render under an error heading. renamed `error` → `detail` in both renderers, carrying whichever detail matches the state.
- advisory: explicit `--format=` on all three `Show*` shapes so the placeholder header stays out of the LLM input and NUL parser.
- round 7 launched; expecting the flip.

## commit-policy arc, mercurius round 7

- verdict `needs_changes` with exactly one concern and one advisory; reviewer says one command-level correction closes the remaining blocker. both taken.
- CP-R7-001: `git show`'s default combined merge diff omits paths matching either parent — an operator-started merge completed under `all` would describe as nearly nothing. all three `Show*` commands now carry `--diff-merges=first-parent`; merge test (differs from first parent, matches second) pinned.
- advisory: NUL name-status parser tests cover two-path `R`/`C` records — score token, both paths, destination categorization.
- round 8 launched.
